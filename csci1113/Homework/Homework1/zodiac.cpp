#include <iostream>
using namespace std;

int main() {
    int month = 0;
    int day = 0;
    char temp = ' ';
    //enter info
    cout << "Enter when you were born in dd/mm format ";
    cin >> day >> temp >> month;
    if (month>0 || month <12)
    {
        switch (expression)
        {
        case month ==1:
            /* code */
            break;
        
        default:
            break;
        }
    }
    
}